apiVersion : v1
Kind : pod
metadata :
spec :
  containers :
    - name : 
      image :
      port :
    - env :
      resource :
      volume :
      livenessprobe :
      readnessprobe :
  Volumes :
  Node rules :
  RestartPolicy :

  #simple

  apiVersion :
  kind :
  metadata :
  spec :
    containers :
     - name : ubuntu
       image : ubuntu:latest
       ports : 8080

       env :
       resource :
       volume :
       livenessProbe :
       readinessProbe :


# pod spec - three tiers 
# NIP = N - NAME , I - IMAGE , P - PORT
# ERVLR = E - ENV , R - RESOURCES , V - VOLUME , L - LIVENESS PROBE , R - READNESS PROBE
# VLR = V - Volumes, L -  Node,R - Restart